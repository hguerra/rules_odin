# Release pipeline: full test matrix → source archive → SLSA attestation → GitHub Release.
#
# Triggered by:
#   - Tag push (v*.*.*) — manual releases
#   - workflow_call from tag.yaml — automated releases
#
# Pipeline:
#   test (4 OS runners, Bazel 9.2.0) → release (release_ruleset.yaml)
#                                      → finalize (undraft the GitHub Release)

name: Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_call:
    inputs:
      tag_name:
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  # ---------------------------------------------------------------------------
  # Full OS matrix — validates the tagged commit with Bazel 9.2.0.
  # ---------------------------------------------------------------------------
  test:
    name: Test (${{ matrix.os }}, Bazel 9.2.0)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.tag_name || github.ref }}

      - uses: bazel-contrib/setup-bazel@c5acdfb288317d0b5c0bbd7a396a3dc868bb0f86 # 0.19.0
        with:
          bazelisk-cache: true
          disk-cache: release-${{ matrix.os }}-9.2.0
          repository-cache: true

      - name: Pin Bazel version
        shell: bash
        run: echo "9.2.0" > .bazelversion

      - name: Build rules_odin
        shell: bash
        run: bazel build //...

      - name: Smoke test (e2e)
        shell: bash
        working-directory: e2e/smoke
        run: bazel build --lockfile_mode=error //...

  # ---------------------------------------------------------------------------
  # Release: calls the shared bazel-contrib release_ruleset workflow.
  # Skips its built-in test (already validated by matrix above).
  # Creates a DRAFT GitHub Release with SLSA L2 attestations.
  # ---------------------------------------------------------------------------
  release:
    name: Release
    needs: test
    uses: bazel-contrib/.github/.github/workflows/release_ruleset.yaml@v7.7.0
    with:
      release_files: rules_odin-*.tar.gz
      prerelease: false
      draft: true
      generate_release_notes: true
      bazel_test_command: "echo 'Tests already passed in matrix'"
      tag_name: ${{ inputs.tag_name || github.ref_name }}

  # ---------------------------------------------------------------------------
  # Finalize: publish the draft release after attestations are attached.
  # ---------------------------------------------------------------------------
  finalize:
    name: Publish release
    needs: release
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ inputs.tag_name || github.ref_name }}
        run: gh release edit "$TAG" --draft=false
